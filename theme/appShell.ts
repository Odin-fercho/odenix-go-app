/**
 * Fondo global unificado (web + nativo) — modo claro.
 */
export const APP_BACKGROUND = '#FFFFFF';

/** Texto principal sobre fondo blanco. */
export const TEXT_PRIMARY = '#1A1A1A';

/** Texto secundario / etiquetas. */
export const TEXT_SECONDARY = '#5C5C5C';

/** Borde sutil para tarjetas sobre blanco. */
export const CARD_BORDER = '#EEEEEE';
